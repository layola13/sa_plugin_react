# sa_plugin_react Progress

Overall progress: 99.99999999999997%

## Completed Feature Checkpoints

- 99.99999999999998%: Added generic SAX component source composition to `sa react check`, `sa react build`, and `sa react dev` with `--include <file.sax>` / `-I <file.sax>`. The root component source remains first, included SAX files are appended into the same parser/lowerer program, and the feature is intentionally React/SAX-generic so libraries such as `sa_plugin_mui` can provide SA/SAX component sources without adding plugin-specific Zig source expansion. Include paths resolve relative to the root source, `SA_REACT_INCLUDE_PATH`, plugin share directories inferred from `SA_PLUGINS_PATH`, installed plugin share directories, and the local development plugin workspace, so SAX asset plugins can be consumed as paths like `mui/material.sax`. Verified with regression tests for both relative library includes and plugin-share includes, plus full `zig build test -j1`.

- 99.99999999999999%: Browser DOM compatibility tightened for SAX asset libraries such as MUI without adding library-specific Zig logic. The Airlock string-property bridge now handles SVG `className` by writing the `class` attribute instead of assigning the read-only browser SVG `className` object, while HTML nodes keep reflected `className` behavior. Generated bundle CSP now permits the already-supported safe lowered React style attrs, so `style={{ ... }}` output is not blocked at runtime. Verification: `zig test src/react/airlock_gen.zig`, `zig build test -j1`, and MUI's Chromium verifier against a `--include mui/material.sax` bundle.

- 99.99999999999999%: User-component children projection now covers initial text children as well as child element nodes. The lowerer emits a generic `sax_dom_create_text` Airlock import and creates/appends text nodes for text children projected through `<Slot />`, so wrapper components can receive children such as `<Typography>SA driven MUI</Typography>` and `<Button>Save</Button>` without special-case library logic. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig build test -j1`, and MUI's Chromium verifier with projected text assertions.

- 99.99999999999999%: Generic React/SAX template lowering now supports simple static string ternary pieces inside quoted DOM attr templates, such as `className="root{disabled ? ' Mui-disabled' : ''}"`, using verifier-safe flat SA control flow (`br` plus `jmp`) and a shared branch-written length value. Multi-component source composition also emits extern/std imports/array adapters only once, and slot exports now return DOM handles loaded as `i64`. These fixes are library-neutral and allow SA/SAX component libraries to express ownerState-style class branches without adding plugin-specific Zig logic. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig build test -j1`, and MUI's Chromium verifier for disabled Button/ButtonBase ownerState classes.

- 99.99999999999999%: User-component prop lowering now maps parser-normalized React DOM prop aliases back to child component state names before emitting setter calls. This keeps React-style camelCase component props such as `readOnly`, `autoFocus`, and `className` working after the parser normalizes intrinsic DOM spellings to `readonly`, `autofocus`, and `class`, and it remains generic React/SAX behavior rather than MUI-specific logic. Verification: `zig test src/react/lowerer.zig`, `zig build test -j1`, and MUI's Chromium verifier covering Form/Input `readOnly` ownerState and native input properties.

- 46%: Reflected React HTML string DOM properties completed and verified. Covered `htmlFor`, `rowSpan`, `colSpan`, `dateTime`, `charSet`, `srcSet`, `useMap`, `crossOrigin`, `autoComplete`, `acceptCharset`, `encType`, `method`, `target`, `formAction`, `formEncType`, `formMethod`, and `formTarget` through the string property bridge with attribute reflection and safety checks.
- 49%: URL/media/link reflected string DOM properties completed and verified. Covered `src`, `href`, `action`, `poster`, `download`, `rel`, `preload`, `media`, `integrity`, `as`, and `type` for anchors, links, media, sources, forms, and images through the string property bridge with `_is_attr_allowed` URL safety enforcement.
- 51%: React `defaultSelected` semantics completed and verified. Parser now preserves `defaultSelected` instead of normalizing it to `selected`; lowerer writes it only during initial mount, and runtime verification confirms later renders do not reset user-mutated option selection.
- 53%: React `select defaultValue` semantics completed and verified for single and multiple selects. The lowerer now defers initial select default value writes until after all child options are attached, the airlock correctly splits comma/newline multi-select values, and runtime verification confirms later renders do not reset user-mutated select state.
- 55%: Static safe React `dangerouslySetInnerHTML` support completed and verified. Parser accepts only `{{ __html: "..." }}` object literals, rejects scripts/inline handlers/`javascript:` URLs, lowerer emits the dedicated `sax_dom_set_inner_html` bridge instead of unsafe DOM attrs, and runtime verification covers trusted innerHTML writes.
- 56%: React DOM no-op warning props completed and verified. `suppressContentEditableWarning` and `suppressHydrationWarning` are accepted by the parser/lowerer path but skipped during DOM emission, with runtime verification confirming they do not become real DOM attributes.
- 58%: Media/image React DOM property bridge completed and verified. Added property-safe handling for `disablePictureInPicture`, `disableRemotePlayback`, `controlsList`, `loading`, `decoding`, and `fetchPriority`, including airlock allowlists, reflected attribute names, DOM surface runtime assertions, and full `zig build test` verification.
- 60%: Text-entry form React DOM property bridge completed and verified. Added property-safe reflected handling for `placeholder`, `pattern`, `accept`, `capture`, and `dirName` on `input`/`textarea`, including React `dirName` normalization, airlock allowlists, DOM surface assertions, and full `zig build test` verification.
- 61%: React `input multiple` property support completed and verified. The existing SAX multiple bridge now covers `input` as well as `select`, with file/email input runtime assertions and full `zig build test` verification.
- 62%: React `option label` property support completed and verified. `label` on `option` now goes through the reflected string property bridge instead of plain attribute-only emission, with DOM surface property/attribute assertions and full `zig build test` verification.
- 63%: React `optgroup label` property support completed and verified. `label` on `optgroup` now uses the same reflected string property bridge as `option`, with grouped select DOM surface assertions and full `zig build test` verification.
- 64%: React `type` property support for `input` and `button` completed and verified. `type` on form controls now uses the reflected string property bridge rather than attribute-only emission, with lowerer coverage for `input`/`button`, DOM surface runtime assertions, airlock allowlist verification, and full `zig build test` verification.
- 65%: React `textarea wrap` property support completed and verified. `wrap` on `textarea` now uses the reflected string property bridge with lowerer coverage, airlock allowlist verification, DOM surface property/attribute assertions, and full `zig build test` verification.
- 66%: React `select autoComplete` property support completed and verified. `autoComplete` now covers `select` in addition to existing `input`, `textarea`, and `form` handling, with lowerer coverage, DOM surface property/attribute assertions, and full `zig build test` verification.
- 67%: React intrinsic media dimension property support completed and verified. `width` and `height` on `img`, `video`, and `canvas` now use the reflected string property bridge while preserving SVG dimension attributes, with lowerer coverage, airlock allowlist verification, DOM surface video/image assertions, and full `zig build test` verification.
- 68%: React table cell semantic property support completed and verified. `headers` on `td`/`th`, plus `scope` and `abbr` on `th`, now use the reflected string property bridge with lowerer coverage, airlock allowlist verification, DOM surface property/attribute assertions, and full `zig build test` verification.
- 69%: React ordered list `start` property support completed and verified. `start` on `ol` now uses the reflected string property bridge with lowerer coverage, airlock allowlist verification, DOM surface property/attribute assertions, and full `zig build test` verification.
- 70%: React list item `value` property support completed and verified. `value` on `li` now uses the reflected string property bridge without routing through the form-control value bridge, with lowerer coverage, DOM surface property/attribute assertions, and full `zig build test` verification.
- 71%: React text citation URL property support completed and verified. `cite` on `blockquote`, `q`, `del`, and `ins` now uses the reflected string property bridge while remaining covered by the airlock URL safety allowlist, with lowerer coverage, DOM surface property/attribute assertions, and full `zig build test` verification.
- 72%: React edit timestamp property support completed and verified. `dateTime` on `del` and `ins` now uses the reflected string property bridge in addition to existing `time` support, with lowerer coverage, DOM surface property/attribute assertions, and full `zig build test` verification.
- 73%: React ordered list `reversed` property support completed and verified. `reversed` on `ol` now uses the boolean property bridge with lowerer coverage, airlock allowlist verification, DOM surface property assertions, and full `zig build test` verification.
- 74%: React table column `span` property support completed and verified. `span` on `col` and `colgroup` now uses the reflected string property bridge with lowerer coverage, airlock allowlist verification, DOM surface property/attribute assertions, and full `zig build test` verification.
- 75%: React image `alt` and global `title` property support completed and verified. `alt` on `img` plus `title` on intrinsic DOM nodes now use the reflected string property bridge with ptr-state/template lowerer coverage, airlock allowlist verification, DOM surface property/attribute assertions, and full `zig build test` verification.
- 76%: React responsive image `sizes` property support completed and verified. `sizes` on `img` and `source` now uses the reflected string property bridge alongside existing `srcSet` support, with lowerer coverage, airlock allowlist verification, DOM surface property/attribute assertions, and full `zig build test` verification.
- 77%: React `referrerPolicy` property support completed and verified. `referrerPolicy` on `img`, `a`, and `link` now uses the reflected string property bridge with React camelCase parser normalization, lowerer coverage, DOM surface property/attribute assertions, and full `zig build test` verification.
- 78%: React media `track` property support completed and verified. `src`, `kind`, `srcLang`, and `label` on `track` now use the reflected string property bridge with React camelCase parser normalization for `srcLang`, lowerer coverage, DOM surface property/attribute assertions, and full `zig build test` verification.
- 79%: React global string DOM property support completed and verified. `lang`, `accessKey`, `slot`, `part`, and `popover` now use the reflected string property bridge with airlock allowlist enforcement, `accessKey` attribute reflection, lowerer coverage, DOM surface property/attribute assertions, and full `zig build test` verification.
- 80%: React global navigation and accessibility property support completed and verified. `dir`, `role`, and `tabIndex` now use the reflected string property bridge with parser normalization for `tabIndex`, airlock allowlist enforcement, lowerer coverage, DOM surface property/attribute assertions, and full `zig build test` verification.
- 81%: React global identity property support completed and verified. `id` and `name` now use the reflected string property bridge with lowerer coverage, airlock allowlist enforcement, DOM surface property/attribute assertions, and full `zig build test` verification.
- 82%: React `translate` property support completed and verified. `translate` now uses a dedicated DOM bridge instead of the generic string/bool paths, normalizing DOM property semantics to `translate=true/false` with reflected `translate="yes"` / `translate="no"`, plus lowerer coverage, runtime DOM surface assertions, and full `zig build test` verification.
- 83%: React global `nonce` property support completed and verified. `nonce` now uses the reflected string property bridge with airlock allowlist enforcement, lowerer coverage, DOM surface property/attribute assertions, and full `zig build test` verification.
- 84%: React `className` property support completed and verified. Parser-normalized `className` / `class` now uses the reflected string property bridge, airlock reflects `className` through the `class` attribute, the runtime shim keeps `className`, `class`, and `classList` synchronized, and the DOM surface verifier asserts property/attribute/token behavior. The WASM verifier now treats `sax_dom_set_attr` as usage-driven instead of globally required, matching demos that no longer need plain attribute writes after class lowering moved to the property bridge. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test`.
- 85%: React microdata DOM property support completed and verified. Parser normalization now covers `itemProp`, `itemScope`, `itemType`, `itemID`, and `itemRef`; `itemScope` uses the generic boolean property bridge, and `itemProp` / `itemType` / `itemID` / `itemRef` use the reflected string property bridge with airlock allowlist and attribute-name mapping. The runtime shim models the microdata properties, and the DOM surface verifier asserts microdata property plus string-attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test`.
- 86%: React `value` property support for `option` and `button` completed and verified. `input` / `textarea` / `select` still use the controlled value bridge, while `option value` and `button value` now use the reflected string property bridge so DOM properties and `value` attributes stay aligned. Lowerer coverage now asserts option and button value property calls, and the DOM surface verifier checks option/button property plus reflected attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test`.
- 87%: React global editing hint string property support completed and verified. `inputMode`, `enterKeyHint`, `autoCapitalize`, and `autoCorrect` now use the reflected string property bridge on intrinsic DOM nodes, not only `input`, while preserving local `autocorrect` bridge naming. Lowerer coverage now asserts the hints on a content-editable `div`, and the DOM surface verifier checks editable-region DOM properties plus normalized `inputmode`, `enterkeyhint`, `autocapitalize`, and `autocorrect` attributes. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test`.
- 88%: React image submit input DOM property support completed and verified. `src`, `alt`, `width`, and `height` on `input` now use the reflected string property bridge, so image-submit controls align with React DOM property behavior while ordinary non-media elements still use plain attribute emission for those names. Lowerer coverage asserts the image input property calls and the negative `div` case, and the DOM surface verifier checks image input properties plus reflected attributes. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test`.
- 89%: React image map `area` DOM property support completed and verified. `alt`, `href`, `download`, `rel`, `target`, and `referrerPolicy` on `area` now use the reflected string property bridge, sharing the existing URL/referrer safety path used by anchors and images while unrelated elements still emit those names as plain attributes. Lowerer coverage asserts the area property calls and a negative `div` case, and the DOM surface verifier checks image-map area properties plus reflected attributes. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 90%: React image map `area` geometry property support completed and verified. `coords` and `shape` on `area` now use the reflected string property bridge alongside the existing area URL/text props, with airlock string-property allowlist coverage and runtime shim reflection for both properties. Lowerer coverage asserts the area geometry property calls, and the DOM surface verifier checks `coords` / `shape` properties plus reflected attributes. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 91%: React image `isMap` boolean DOM property support completed and verified. Parser normalization now maps React `isMap` to the intrinsic `ismap` attr name, and `img isMap` uses the generic boolean property bridge with airlock allowlist support while non-image elements remain on the plain attribute path. The DOM surface verifier checks the image `isMap` property after mount. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 92%: React `data` element `value` property support completed and verified. `value` on `<data>` now uses the reflected string property bridge, aligning it with React DOM behavior for the HTML data element while unrelated elements still emit `value` through the plain attribute path. Lowerer coverage asserts the `data` property call and negative `div` case, and the DOM surface verifier checks the mounted `data.value` property plus reflected `value` attribute. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 93%: React `output` form association property support completed and verified. `htmlFor` / normalized `for` on `<output>` now uses the reflected `htmlFor` string property bridge alongside existing label support, while unrelated elements still emit `htmlFor` through the plain attribute path. Lowerer coverage asserts the output property call and negative `div` case, and the DOM surface verifier checks the mounted output `htmlFor` property plus reflected `for` attribute. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 94%: React `hrefLang` reflected link property support completed and verified. Parser normalization now maps React `hrefLang` to `hreflang`, and `a`, `area`, and `link` elements route it through the reflected string property bridge with airlock allowlist support. The runtime shim models the `hreflang` property, and the DOM surface verifier checks property plus reflected `hreflang` attributes for anchor, image-map area, and link elements. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 95%: React `meta` metadata property support completed and verified. Parser normalization now maps React `httpEquiv` to `http-equiv`, and `<meta>` routes `httpEquiv` plus `content` through the reflected string property bridge with airlock allowlist and `http-equiv` attribute mapping. The runtime shim models both properties, and the DOM surface verifier checks property plus reflected attribute behavior while keeping non-meta `content` on the plain attribute path. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 96%: React anchor/link `type` MIME property support completed and verified. `<a type>` and `<link type>` now route through the reflected string property bridge, so MIME hints update DOM properties plus reflected `type` attributes while non-anchor/link cases such as `<area type>` remain on the plain attribute path. DOM surface verification checks anchor and link `type` property/attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 97%: React `style` element media metadata property support completed and verified. `<style media>` and `<style type>` now route through the reflected string property bridge, so stylesheet metadata updates DOM properties plus reflected `media` / `type` attributes while unrelated elements such as `<div media>` remain on the plain attribute path. DOM surface verification checks style element property/attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 98%: React `param` value property support completed and verified. `<param value>` now routes through the reflected string property bridge, so parameter values update the DOM `value` property plus reflected `value` attribute while unrelated elements such as `<div value>` remain on the plain attribute path. DOM surface verification checks `param` name/value property and attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99%: React `base` URL metadata property support completed and verified. `<base href>` and `<base target>` now route through the reflected string property bridge, so document base URL metadata updates DOM properties plus reflected `href` / `target` attributes while unrelated elements such as `<div href target>` remain on the plain attribute path. DOM surface verification checks base element property/attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.5%: React ordered list `type` marker property support completed and verified. `<ol type>` now routes through the reflected string property bridge, so ordered-list marker type updates the DOM `type` property plus reflected `type` attribute while unrelated list elements such as `<ul type>` remain on the plain attribute path. DOM surface verification checks `ol` start/type property and attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.6%: React `source` media dimension property support completed and verified. `<source width>` and `<source height>` now route through the reflected string property bridge alongside existing source `src` / `srcSet` / `sizes` / `type` / `media` support, so responsive media source dimensions update DOM properties plus reflected `width` / `height` attributes while unrelated elements such as `<div width height>` remain on the plain attribute path. DOM surface verification checks source dimension property and attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.7%: React form `rel` property support completed and verified. `<form rel>` now routes through the reflected string property bridge alongside existing form `action` / `acceptCharset` / `encType` / `method` / `target` / `autoComplete` support, so form link relation metadata updates the DOM `rel` property plus reflected `rel` attribute while unrelated elements such as `<div rel>` remain on the plain attribute path. DOM surface verification checks form `rel` property and attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.8%: React table column `width` property support completed and verified. `<col width>` and `<colgroup width>` now route through the reflected string property bridge alongside existing column `span` support, so table column sizing metadata updates DOM `width` properties plus reflected `width` attributes while unrelated elements remain on the plain attribute path. DOM surface verification checks colgroup/col `span` and `width` property plus attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9%: React table cell dimension property support completed and verified. `<td width height>` and `<th width height>` now route through the reflected string property bridge alongside existing cell `rowSpan` / `colSpan` / `headers` and header `scope` / `abbr` support, so table cell sizing metadata updates DOM `width` / `height` properties plus reflected attributes. DOM surface verification checks td/th dimension property and attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.95%: React image `longDesc` URL property support completed and verified. React `longDesc` now normalizes through the parser, `<img longDesc>` routes through the reflected string property bridge as DOM `longDesc`, and Airlock reflects it as the `longdesc` attribute with URL safety enforcement alongside `href` / `src` / `poster` / `cite`. Non-image elements such as `<div longDesc>` remain on the plain attribute path. DOM surface verification checks image `longDesc` property plus reflected `longdesc` attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.96%: React link `disabled` property support completed and verified. `<link disabled>` now routes through the generic boolean DOM property bridge while existing form-control `disabled` handling remains on the dedicated disabled bridge and unrelated elements such as `<section disabled>` stay on the plain attribute path. Airlock allowlists the boolean `disabled` property, and DOM surface verification checks the mounted link `disabled` DOM property. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.97%: React link responsive image preload metadata support completed and verified. React `imageSrcSet` / `imageSizes` now normalize through the parser, `<link imageSrcSet imageSizes>` routes through the reflected string property bridge as DOM `imageSrcset` / `imageSizes`, and Airlock reflects them as `imagesrcset` / `imagesizes` attributes. Non-link elements such as `<div imageSrcSet imageSizes>` remain on the plain attribute path. DOM surface verification checks preload link properties plus reflected attributes. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.98%: React link `fetchPriority` preload priority support completed and verified. `<link fetchPriority>` now routes through the reflected string property bridge alongside existing link preload metadata, so resource hints update the DOM `fetchPriority` property plus reflected `fetchpriority` attribute while unrelated elements such as `<div fetchPriority>` remain on the plain attribute path. DOM surface verification checks preload link priority property and attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99%: React link `charSet` metadata property support completed and verified. React `charSet` now normalizes through the existing parser path and `<link charSet>` routes through the reflected string property bridge as DOM `charset`, so preload/resource link charset metadata updates the DOM property plus reflected `charset` attribute while unrelated elements such as `<div charSet>` remain on the plain attribute path. DOM surface verification checks preload link charset property and attribute behavior. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.995%: React anchor/area `ping` URL-list property support completed and verified. `<a ping>` and `<area ping>` now route through the reflected string property bridge as DOM `ping`, while unrelated elements such as `<div ping>` remain on the plain attribute path. Airlock allowlists the reflected `ping` property, treats `ping` as a URL attribute, and rejects `javascript:` tokens inside whitespace-separated ping URL lists. DOM surface verification checks anchor and image-map area `ping` properties plus reflected `ping` attributes, and the security verifier checks mixed safe/unsafe ping token rejection. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, `zig build test -j1`, and `node tools/verify_react_runtime.mjs /tmp/sa-react-dom-surface-ping security` after rebuilding the demo output.
- 99.996%: React anchor `charSet` metadata property support completed and verified. React `charSet` already normalizes through the parser to `charset`, and `<a charSet>` now routes through the reflected string property bridge as DOM `charset`, so anchor charset metadata updates the DOM property plus reflected `charset` attribute while non-anchor elements such as `<div charSet>` remain on the plain attribute path. DOM surface verification checks the anchor `charset` property and reflected `charset` attribute. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.997%: React anchor geometry metadata property support completed and verified. `<a coords shape>` now routes through the reflected string property bridge as DOM `coords` / `shape`, matching the existing image-map area bridge while unrelated elements such as `<div coords shape>` remain on the plain attribute path. DOM surface verification checks anchor `coords` / `shape` properties plus reflected attributes. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.998%: React link `sizes` metadata property support completed and verified. `<link sizes>` now routes through the reflected string property bridge as DOM `sizes`, so icon link size metadata updates the DOM property plus reflected `sizes` attribute while unrelated elements such as `<div sizes>` remain on the plain attribute path. DOM surface verification checks icon link `href` / `sizes` / `type` properties plus reflected attributes. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999%: React link `target` metadata property support completed and verified. `<link target>` now routes through the reflected string property bridge as DOM `target`, so link target metadata updates the DOM property plus reflected `target` attribute while unrelated elements such as `<section target>` remain on the plain attribute path. DOM surface verification checks icon link `href` / `sizes` / `type` / `target` properties plus reflected attributes. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9995%: React link `blocking` metadata property support completed and verified. `<link blocking>` now routes through the reflected string property bridge as DOM `blocking`, with Airlock allowlisting the string property and reflecting it through the `blocking` attribute. Unrelated elements such as `<div blocking>` remain on the plain attribute path. DOM surface verification checks preload link `blocking` property plus reflected attribute alongside the existing link preload metadata. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9996%: React user component state refs completed and verified. DOM refs remain typed as `i64` state slots for mounted DOM handles, while user component refs now require `ptr` state slots and store the mounted child component context pointer immediately after `sax_<child>_mount`. `ref` continues to be skipped as a DOM attribute and as a child prop, and ref state writes stay mount-only rather than render-time. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9997%: React callback refs completed for mounted DOM nodes and user components. `ref={handlerName}` now resolves to a dedicated ref callback wrapper when the name is not a state ref, passes `ref_value` as an `i64` DOM handle or `ptr` child component context on mount, and invokes the same wrapper with `0` during destroy for React-style nulling. Ref-only callbacks that depend on `ref_value` no longer emit incompatible plain event handler wrappers. The composition demo now covers DOM and user component callback refs through `react check`, `react build`, WASM export verification, and runtime assertions. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9998%: Static flat React object prop ABI completed for user components. Component props written as `prop={{ key: "value", count: 3, active: true, empty: null }}` now parse as safe flat object literals, normalize to compact JSON text, and lower through the existing child `ptr` state string setter ABI. The composition demo now passes an object prop into `ObjectBadge`, verifies the generated child string setter export, and asserts the mounted runtime text receives the normalized JSON. Nested objects, arrays, and dynamic object expressions remain follow-up ABI work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99985%: Static nested React object and array prop ABI completed for user components. Static object props now recursively normalize nested objects and arrays containing strings, numbers, booleans, and null into compact JSON text, then lower through the child `ptr` state string setter ABI. Parser/lowerer tests cover nested objects and arrays, and the composition demo/runtime verifier now asserts that `ObjectBadge` receives the nested JSON payload. Dynamic object expressions, computed keys, spreads, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999%: Dynamic numeric React object prop values completed for user components. Object props can now include parent numeric state references, including nested positions such as `config={{ count: count, nested: { current: count } }}`. The parser preserves those object props as JSON templates, the lowerer formats the state values into runtime JSON buffers, and child `ptr` state setters receive refreshed object payloads during parent renders. The composition runtime now verifies the object prop changes from `count:0` to `count:1` after clicking the parent button. Computed keys, spreads, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99992%: Dynamic boolean React object prop values completed for user components. Simple `i1` state references inside object props, such as `config={{ active: active }}`, now lower through JSON-specific template output that writes `true` / `false` literals instead of numeric `0` / `1`. Parser and lowerer coverage verifies boolean object prop templates, and the composition runtime now exercises `active:false` on mount and `active:true` after a click. Computed keys, spreads, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99994%: Dynamic ptr-string React object prop values completed for user components. Simple parent `ptr` state references inside object props, such as `config={{ title: title }}`, now route through object-prop metadata and the Airlock `sax_json_write_string` bridge so child string setters receive quoted, JSON-escaped string values instead of raw bytes. Ordinary string prop templates, such as `<Label title="{title}" />`, continue to pass raw string slices. Parser/lowerer tests cover object-prop marking, JSON string bridge calls, and non-object string prop preservation; the composition runtime verifies an object prop title refreshing from `""` to `"click"` after a click. Computed keys, spreads, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99995%: Static computed string keys completed for React user component object props. Object props now accept quoted computed keys such as `config={{ ["title"]: title, nested: { ["current"]: count } }}` and normalize them to the same compact JSON key representation used by ordinary identifier and quoted keys. Parser tests covered static object key normalization and the then-unsupported dynamic key path, the composition demo used computed string keys, and runtime composition verification still observed the expected object JSON updates. General dynamic computed key expressions, spreads, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99996%: Static object spread literals completed for React user component object props. Object props now accept static literal spreads such as `config={{ ["title"]: title, ...{ spread: "static" }, nested: { ...{ enabled: true } } }}` and flatten those members into the same compact JSON/template representation used by ordinary object members. Dynamic spreads such as `...config` remain rejected instead of being treated as unsupported JavaScript object semantics. Parser and lowerer tests cover static spread flattening and dynamic spread rejection, the composition demo now exercises top-level and nested static spreads, and runtime composition verification observes the flattened JSON payload before and after parent state updates. General dynamic computed key expressions, dynamic object spreads, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99997%: Trailing dynamic React object spread support completed for user component object props. Object props now accept trailing simple `ptr` state JSON object spreads such as `config={{ title: title, ...extra_json }}`. The parser preserves the spread as a dedicated object-spread template piece, the lowerer loads the source string state and calls the Airlock `sax_json_write_object_members` bridge, and the bridge only copies members after the source parses as a JSON object. Non-trailing dynamic spreads such as `config={{ ...config, count: 1 }}` remain rejected until full JavaScript object spread overwrite semantics are implemented. Parser/lowerer/plugin validation tests cover the new template piece and rejection path, Airlock/WASM verifier coverage checks the new optional import surface, and the composition demo now emits the dynamic spread bridge while preserving runtime object prop JSON output. General dynamic computed key expressions, non-trailing/full dynamic object spread overwrite semantics, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99998%: Simple dynamic computed string keys completed for React user component object props. Object props now accept simple `ptr` state computed keys such as `config={{ [object_key]: count }}`. The parser preserves these keys as JSON-string template pieces, the lowerer routes them through `sax_json_write_string` so runtime keys are quoted and escaped safely, and composition runtime verification now observes a computed key changing from the empty string key on mount to `"click"` after the parent event updates the backing string state. Parser/lowerer/plugin validation covers the new template piece and dependency tracking, while full demo verification covers generated WASM imports and runtime JSON output. General JavaScript computed key expressions, non-trailing/full dynamic object spread overwrite semantics, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999985%: Scalar dynamic computed keys completed for React user component object props. Computed object keys can now use simple numeric expressions and boolean state values, such as `config={{ [count + 1]: active, [active]: count }}`. Numeric keys are formatted with the existing integer/float formatting path and wrapped as JSON strings, while simple `i1` state keys emit quoted `"true"` / `"false"` literals without losing boolean key semantics to `1` / `0`. Parser coverage now preserves bracket expressions through `]`, lowerer coverage checks numeric-key formatting and boolean-key branching, and the composition runtime verifies keys such as `"1"`, `"false"`, `"2"`, and `"true"` across parent state updates. General JavaScript computed key expressions, non-trailing/full dynamic object spread overwrite semantics, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999%: Dynamic values inside static object spread literals completed for React user component object props. Static spread literals such as `config={{ ...{ spread: "static", spread_count: count }, nested: { ...{ enabled: true, spread_active: active } } }}` now parse their spread-literal body through the dynamic JSON template parser instead of the static-only object parser, so scalar/string interpolations inside flattened spread members reuse the existing object-prop JSON formatting path. Parser and lowerer coverage now verify the added template pieces, the composition demo verifies `spread_count` and `spread_active` before and after parent updates, and the demo target object buffer was increased to fit the larger generated JSON payload without corrupting runtime state. General JavaScript computed key expressions, non-trailing/full dynamic object spread overwrite semantics, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999991%: Post-member non-trailing dynamic React object spreads completed for user component object props. Dynamic `ptr` JSON object spreads may now appear after at least one already-emitted object member and before later ordinary members, such as `config={{ title: title, ...extra_json, count: count }}`. The parser preserves the spread template piece and following text instead of requiring the spread to be final. Lowerer coverage exercises the non-tail bridge call, and the composition demo placed `...object_extra` before later fields while runtime verification still observed stable object JSON across parent updates. General JavaScript computed object key expressions, full dynamic object spread overwrite semantics, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999992%: Leading non-trailing dynamic React object spreads completed for user component object props. Dynamic `ptr` JSON object spreads may now appear at the start of an object before later ordinary members, such as `config={{ ...extra_json, title: title }}`. The parser preserves the leading spread and following members, and the lowerer tracks the Airlock spread write count so the following comma is emitted only when the source JSON object actually contributes members; empty or invalid source strings now fall through to valid JSON instead of producing `{,"key":...}`. Lowerer coverage checks the generated conditional comma branch, and the composition demo now exercises a leading empty `...object_extra` while runtime verification still observes stable object JSON before and after parent updates. General JavaScript computed object key expressions, full dynamic object spread overwrite semantics, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999993%: Non-empty leading dynamic React object spread runtime coverage completed for user component object props. The composition demo now includes a small controlled JSON input that writes into `object_extra`, and runtime verification drives it with `{"extra":"spread","source":2}` before clicking the parent button. This confirms the leading `...object_extra` path copies real source object members before later dynamic/static members, keeps conditional comma behavior valid for the initial empty source, and preserves the spread members across subsequent parent state updates. General JavaScript computed object key expressions, full dynamic object spread overwrite semantics, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999994%: Dynamic array values completed for React user component object props. Arrays nested inside object props can now contain dynamic `ptr` string, numeric, and boolean state values, such as `config={{ tags: ["alpha", title, count, active] }}`. Parser coverage preserves the array elements as JSON template pieces, lowerer coverage checks JSON string escaping plus numeric and boolean literal formatting, and the composition runtime now verifies tags changing from `["alpha","",0,false]` to `["alpha","click",1,true]` while preserving leading spread members. General JavaScript computed object key expressions, full dynamic object spread overwrite semantics, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999995%: Object prop spread overwrite normalization completed for React user component object props. Static and dynamic object-prop JSON now pass through the Airlock `sax_json_normalize_object` bridge before child string setters receive them, so duplicate keys produced by spreads or computed keys follow JavaScript object semantics where later writes overwrite earlier values. The WASM verifier now checks the new optional import and Airlock surface, lowerer coverage checks static and template object props route through normalization, and the composition runtime verifies a leading `...object_extra` source containing `title`, `count`, and `tags` is overwritten by later object members while preserving unrelated spread members. General JavaScript computed object key expressions, arbitrary JavaScript object spread expressions, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999996%: Parenthesized scalar arithmetic computed keys completed for React user component object props. Computed object keys now have parser, lowerer, and composition runtime coverage for expressions such as `[((count + 2) * 3) - 1]`, exercising parenthesized addition, multiplication, and subtraction through the runtime JSON key formatting path. The composition demo verifies numeric keys such as `"5"` on mount and `"8"` after parent state updates while preserving object spread overwrite normalization and boolean/string computed keys. General JavaScript computed object key expressions beyond the current scalar arithmetic/state subset, arbitrary JavaScript object spread expressions, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999997%: Boolean string ternary computed keys completed for React user component object props. Computed object keys now support the simple JavaScript expression form `i1_state ? "key" : "key"`, such as `[active ? "enabled" : "disabled"]`, branching at render time between pre-escaped JSON string keys before object-prop normalization. Parser coverage preserves the ternary expression, lowerer coverage checks the generated `KEY_TERNARY` branch and quoted key constants, and the composition runtime verifies the object prop key changes from `"disabled"` on mount to `"enabled"` after parent state updates while preserving spread overwrite normalization and existing numeric/boolean/string computed keys. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-ternary subset, arbitrary JavaScript object spread expressions, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999998%: Boolean ternary dynamic object spread sources completed for React user component object props. Dynamic object spreads now accept the simple JavaScript expression form `i1_state ? ptr_json : ptr_json`, such as `...(active ? object_branch : object_empty)`, choosing between two parent `ptr` JSON object state buffers at render time before calling the existing `sax_json_write_object_members` bridge. Parser coverage preserves parenthesized ternary spread expressions, lowerer coverage checks the generated `OBJ_TERNARY` branch and selected source writes, and the composition demo now drives a branch JSON input that is only spread after parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state and simple boolean-ternary ptr-state subset, complex non-scalar object/string expressions, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999%: Boolean ternary ptr-string object values completed for React user component object props. Object-prop values now support the simple JavaScript expression form `i1_state ? ptr_string : ptr_string`, such as `status: active ? object_title : status_fallback`, branching at render time and JSON-escaping the selected parent `ptr` string through `sax_json_write_string`. Parser coverage preserves full identifier-starting value expressions instead of truncating at the first identifier, lowerer coverage checks the generated `VALUE_TERNARY` branch, and the composition runtime verifies `status` changes from the fallback string `"idle"` to the active event-derived string `"click"` while preserving object spread normalization and computed-key behavior. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state and simple boolean-ternary ptr-state subset, complex non-scalar object/string expressions beyond the current ptr-state and simple boolean-ternary ptr-state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999991%: Boolean ternary i64 object values completed for React user component object props. Object-prop values now support the simple JavaScript expression form `i1_state ? i64_state : i64_state`, such as `score: active ? count : idle_score`, branching at render time and formatting the selected numeric state through `sax_itoa` into the JSON template buffer. Parser coverage preserves the full ternary value expression, lowerer coverage checks the generated `I64_TERNARY` branch and selected numeric writes, and the composition runtime verifies `score` changes from the fallback numeric state `9` to the active `count` value `1` while preserving object spread normalization, string ternary values, and computed-key behavior. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state and simple boolean-ternary ptr-state subset, complex non-scalar object/string expressions beyond the current ptr-state, numeric-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999992%: Boolean ternary i1 object values completed for React user component object props. Object-prop values now support the simple JavaScript expression form `i1_state ? i1_state : i1_state`, such as `visible: active ? active : fallback_active`, branching at render time and writing the selected boolean state as JSON `true` / `false` literals. Parser coverage preserves the full ternary value expression, lowerer coverage checks the generated `I1_TERNARY` branch and selected boolean writes, and the composition runtime verifies `visible` changes from fallback `false` to active `true` while preserving object spread normalization, numeric/string ternary values, and computed-key behavior. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state and simple boolean-ternary ptr-state subset, complex non-scalar object/string expressions beyond the current ptr-state, numeric/boolean-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999993%: Boolean ternary values inside nested object-prop arrays completed for React user component object props. Arrays nested in object props now exercise the same simple ternary value support as object members for ptr-string, i64, and i1 state branches, such as `variants: [active ? object_title : status_fallback, active ? count : idle_score, active ? active : fallback_active]`. Parser coverage preserves ternary expressions inside array elements, lowerer coverage checks the existing `VALUE_TERNARY`, `I64_TERNARY`, and `I1_TERNARY` paths from array positions, and the composition runtime verifies `variants` changes from `["idle",9,false]` to `["click",1,true]` while preserving object spread normalization and computed-key behavior. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state and simple boolean-ternary ptr-state subset, complex non-scalar object/string expressions beyond the current ptr-state, numeric/boolean-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999994%: Boolean ternary ptr-string computed keys completed for React user component object props. Computed object keys now support the simple JavaScript expression form `i1_state ? ptr_string : ptr_string`, such as `[active ? object_title : status_fallback]: count`, branching at render time and JSON-escaping the selected parent `ptr` string through `sax_json_write_string` before object-prop normalization. Parser coverage preserves the computed-key ternary expression, lowerer coverage checks the generated `KEY_PTR_TERNARY` branch and selected key writes, and the composition runtime verifies the false branch can produce a runtime key such as `"idle"` while the true branch preserves existing dynamic key normalization. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state and simple boolean-ternary ptr-state subset, complex non-scalar object/string expressions beyond the current ptr-state, numeric/boolean-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999995%: Boolean ternary object spread static-literal branches completed for React user component object props. Dynamic object spreads now support simple ternary sources where either branch may be a static object literal, such as `...(active ? object_branch : { idle_branch: true })`, compacting the literal to JSON at parse/lower time and then copying selected object members through the existing Airlock `sax_json_write_object_members` bridge. Parser coverage preserves parenthesized ternary spread expressions containing object literals, lowerer coverage checks static JSON constants inside `OBJ_TERNARY`, and composition runtime verification observes the false branch contributing `"idle_branch":true` while the true branch still selects the dynamic JSON state source after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, numeric/boolean-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999996%: Boolean ternary string-literal object values completed for React user component object props. Object-prop values now support simple ternary string branches where either branch may be a static string literal and the other may be a `ptr` string state, such as `label: active ? "ready" : status_fallback`, copying static branches as pre-escaped JSON strings and continuing to JSON-escape selected `ptr` branches through `sax_json_write_string`. Parser coverage preserves the string-literal ternary value expression, lowerer coverage checks the generated `VALUE_TERNARY` branch and quoted string constant, and composition runtime verification observes `label` changing from the fallback ptr value `"idle"` to the static literal `"ready"` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999997%: Boolean ternary string-literal array values completed for React user component object props. Arrays nested in object props now have parser, lowerer, and composition runtime coverage for simple ternary string branches that mix static string literals with `ptr` string state, such as `variants: [active ? "ready" : status_fallback]`, reusing the same JSON string escaping/copy path as object member values. Parser coverage preserves the array-position string-literal ternary expression, lowerer coverage checks the generated `VALUE_TERNARY` branch and quoted string constant from an array element, and composition runtime verification observes `variants` changing from `["idle",9,false,"idle"]` to `["click",1,true,"ready"]` after parent state updates. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999998%: Boolean ternary mixed string computed keys completed for React user component object props. Computed object keys now support simple ternary branches that mix static string literals with `ptr` string state, such as `[active ? "ready" : status_fallback]: count`, while preserving the existing literal-literal and ptr-ptr ternary key behavior through the same mixed key branch path. Static key branches copy pre-escaped JSON strings, ptr key branches continue to JSON-escape the selected state string through `sax_json_write_string`, and composition runtime verification observes the active branch adding a `"ready":1` key after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999%: Boolean ternary i64 computed keys completed for React user component object props. Computed object keys now support the simple JavaScript expression form `i1_state ? i64_state : i64_state`, such as `[active ? count : idle_score]: active`, branching at render time and formatting the selected numeric state as a quoted JSON object key through `sax_itoa`. Parser coverage preserves the numeric ternary key expression, lowerer coverage checks the generated `KEY_I64_TERNARY` branch and selected key writes, and composition runtime verification observes the false branch adding `"9":false` on mount and the active branch adding `"1":true` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999991%: Boolean ternary i1 computed keys completed for React user component object props. Computed object keys now support the simple JavaScript expression form `i1_state ? i1_state : i1_state`, such as `[active ? active : fallback_active]: idle_score`, branching at render time and writing the selected boolean state as a quoted `"true"` / `"false"` JSON object key. Parser coverage preserves the boolean ternary key expression, lowerer coverage checks the generated `KEY_I1_TERNARY` branch and selected key writes, and composition runtime verification observes the ternary key overwriting the earlier simple `[active]` key from `"false":0` to `"false":9` on mount and from `"true":1` to `"true":9` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i1-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999992%: Boolean ternary f64 computed keys completed for React user component object props. Computed object keys now support the simple JavaScript expression form `i1_state ? f64_state : f64_state`, such as `[active ? ratio : fallback_ratio]: count`, branching at render time and formatting the selected float state as a quoted JSON object key through `sax_ftoa_bits`. Parser coverage preserves the float ternary key expression, lowerer coverage checks the generated `KEY_F64_TERNARY` branch and selected key writes, and composition runtime verification observes the false branch adding `"0.500000":0` on mount and the active branch adding `"0.750000":1` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i1-ternary/f64-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean-state, float-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999993%: Boolean ternary f64 object and array values completed for React user component object props. Object-prop values now support the simple JavaScript expression form `i1_state ? f64_state : f64_state`, such as `precision: active ? ratio : fallback_ratio`, and arrays nested in object props reuse the same path for entries such as `variants: [active ? ratio : fallback_ratio]`. The lowerer formats the selected float state through `sax_ftoa_bits` into the JSON template buffer before object normalization, parser coverage preserves the f64 ternary value expression in object members and arrays, lowerer coverage checks the generated `F64_TERNARY` branch and selected value writes, and composition runtime verification observes `precision` plus the f64 array entry changing from `0.5` to `0.75` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i1-ternary/f64-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean-state, float-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999994%: Boolean ternary i32 object and array values completed for React user component object props. Object-prop values now support the simple JavaScript expression form `i1_state ? i32_state : i32_state`, such as `rating: active ? small_score : fallback_small_score`, and arrays nested in object props reuse the same path for entries such as `variants: [active ? small_score : fallback_small_score]`. The lowerer sign-extends the selected `i32` state to `i64` before formatting through `sax_itoa`, parser coverage preserves the i32 ternary value expression in object members and arrays, lowerer coverage checks the generated `I32_TERNARY` branch and selected value writes, and composition runtime verification observes `rating` plus the i32 array entry changing from `2` to `3` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i1-ternary/f64-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean/float/i32-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999995%: Boolean ternary i32 computed keys completed for React user component object props. Computed object keys now support the simple JavaScript expression form `i1_state ? i32_state : i32_state`, such as `[active ? small_score : fallback_small_score]: active`, branching at render time and formatting the selected signed 32-bit state as a quoted JSON object key. The lowerer sign-extends the selected `i32` state to `i64` before formatting through `sax_itoa`, parser coverage preserves the i32 ternary key expression, lowerer coverage checks the generated `KEY_I32_TERNARY` branch and selected key writes, and composition runtime verification observes the false branch adding `"2":false` on mount and the active branch adding `"3":true` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i32-ternary/i1-ternary/f64-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean/float/i32-state, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999996%: Boolean ternary i64 literal branch object and array values completed for React user component object props. Object-prop values now support simple `i1_state ? i64_state : i64_literal` and `i1_state ? i64_literal : i64_state` branches, such as `bonus: active ? count : 0`, and arrays nested in object props reuse the same path for entries such as `variants: [active ? count : 0]`. The lowerer keeps i64 state branches on the existing state-load plus `sax_itoa` path and formats literal branches directly through `sax_itoa`, parser coverage preserves the literal-branch ternary expression in object members and arrays, lowerer coverage checks the generated `I64_TERNARY` path, and composition runtime verification observes `bonus` plus the array entry changing from `0` to `1` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i32-ternary/i1-ternary/f64-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean/float/i32-state, simple numeric literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999997%: Boolean ternary i64 literal branch computed keys completed for React user component object props. Computed object keys now support simple `i1_state ? i64_state : i64_literal` and `i1_state ? i64_literal : i64_state` branches, such as `[active ? count : 0]: count`, branching at render time and formatting either the selected i64 state or literal as a quoted JSON object key through `sax_itoa`. Parser coverage preserves the literal-branch computed-key expression, lowerer coverage checks the generated `KEY_I64_TERNARY` path, and composition runtime verification observes the false branch adding a normalized `"0":0` key on mount and the active branch overwriting the existing `"1"` key with `1` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i1-ternary/f64-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean/float/i32-state, simple numeric literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999998%: Boolean ternary f64 literal branch object and array values completed for React user component object props. Object-prop values now support simple `i1_state ? f64_state : f64_literal` and `i1_state ? f64_literal : f64_state` branches, such as `precision_floor: active ? ratio : 0.25`, and arrays nested in object props reuse the same path for entries such as `variants: [active ? ratio : 0.25]`. The lowerer parses float literals into f64 bit patterns and formats either selected state bits or literal bits through `sax_ftoa_bits`, parser coverage preserves literal-branch f64 ternary expressions in object members and arrays, lowerer coverage checks the generated `F64_TERNARY` path, and composition runtime verification observes `precision_floor` plus the array entry changing from `0.25` to `0.75` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i1-ternary/f64-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean/float/i32-state, simple numeric/float literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999%: Boolean ternary f64 literal branch computed keys completed for React user component object props. Computed object keys now support simple `i1_state ? f64_state : f64_literal` and `i1_state ? f64_literal : f64_state` branches, such as `[active ? ratio : 0.25]: count`, branching at render time and formatting either selected f64 state bits or literal bits as a quoted JSON object key through `sax_ftoa_bits`. Parser coverage preserves the literal-branch f64 computed-key expression, lowerer coverage checks the generated `KEY_F64_TERNARY` path, and composition runtime verification observes the false branch adding a normalized `"0.250000":0` key on mount while the active branch selects the existing `"0.750000":1` key after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i1-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean/float/i32-state, simple numeric/float literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999991%: Boolean ternary i32 literal branch object and array values completed for React user component object props. Object-prop values now support simple `i1_state ? i32_state : i32_literal` and `i1_state ? i32_literal : i32_state` branches, such as `rating_floor: active ? small_score : 4`, and arrays nested in object props reuse the same path for entries such as `variants: [active ? small_score : 4]`. The lowerer parses integer literals within i32 range, emits literal branches as i32 values, sign-extends the selected branch to i64, and formats through `sax_itoa`; parser coverage preserves literal-branch i32 ternary expressions in object members and arrays, lowerer coverage checks the generated `I32_TERNARY` path, and composition runtime verification observes `rating_floor` plus the array entry changing from `4` to `3` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i1-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean/float/i32-state, simple numeric/i32/float literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999992%: Boolean ternary i32 literal branch computed keys completed for React user component object props. Computed object keys now support simple `i1_state ? i32_state : i32_literal` and `i1_state ? i32_literal : i32_state` branches, such as `[active ? small_score : 4]: active`, branching at render time and formatting either the selected signed 32-bit state or literal as a quoted JSON object key. The lowerer parses literal branches within i32 range, sign-extends the selected branch to i64 before `sax_itoa`, parser coverage preserves the literal-branch computed-key expression, lowerer coverage checks the generated `KEY_I32_TERNARY` path, and composition runtime verification observes the false branch adding `"4":false` on mount while the active branch selects the existing `"3":true` key after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean/float/i32-state, simple numeric/i32/float literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999993%: Boolean ternary i1 literal branch object and array values completed for React user component object props. Object-prop values and arrays nested inside object props now support simple `i1_state ? i1_state : i1_literal`, `i1_state ? i1_literal : i1_state`, and literal/literal boolean branch forms, such as `pinned: active ? true : fallback_active` and `variants: [active ? false : true]`. The lowerer reuses the existing JSON boolean write path by resolving selected branches to i1 values before copying `true` / `false` text, parser coverage preserves literal-branch boolean ternary expressions, lowerer coverage checks generated `I1_TERNARY` output with i1 literals, and composition runtime verification observes `pinned` changing from `false` to `true` while the array boolean entry changes from `true` to `false` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999994%: Boolean ternary i1 literal branch computed keys completed for React user component object props. Computed object keys now support simple `i1_state ? i1_state : i1_literal`, `i1_state ? i1_literal : i1_state`, and literal/literal boolean branch forms, such as `[active ? fallback_active : true]: count`, branching at render time and writing the selected boolean branch as a quoted `"true"` / `"false"` JSON object key. The lowerer now shares the same `state`/`literal` i1 branch parser used by object and array values, parser coverage preserves the literal-branch computed-key expression, lowerer coverage checks generated `KEY_I1_TERNARY` output with i1 literals, and composition runtime verification observes the false condition adding `"true":0` on mount while the active condition adds `"false":1` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999995%: Boolean ternary null literal branch object and array values completed for React user component object props. Object-prop values and arrays nested inside object props now support simple ternary branches that mix `ptr` string state with `null`, such as `nullable: active ? object_title : null` and `variants: [active ? null : status_fallback]`. The lowerer treats `null` as a static JSON value fragment in the existing string-value ternary branch path while continuing to JSON-escape selected `ptr` string state through `sax_json_write_string`; parser coverage preserves null-literal ternary expressions, lowerer coverage checks generated `VALUE_TERNARY` output with the `null` static fragment, and composition runtime verification observes `nullable` changing from `null` to `"click"` while the array entry changes from fallback string values to `null` after the parent state flips active. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999996%: Boolean ternary null literal branch computed keys completed for React user component object props. Computed object keys now support simple ternary branches that mix `ptr` string state with `null`, such as `[active ? status_fallback : null]: count`, branching at render time and coercing the `null` branch to the quoted JSON object key `"null"` while continuing to JSON-escape selected `ptr` string keys. Parser coverage preserves the null-literal computed-key expression, lowerer coverage checks generated `KEY_MIXED_TERNARY` output with the pre-escaped `"null"` key fragment, and composition runtime verification observes the false condition adding `"null":0` on mount while the active condition adds the selected fallback string key `"idle":1` after parent state updates. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999997%: Boolean ternary null branch object spreads completed for React user component object props. Object spread sources now support simple ternary branches that include `null`, such as `...(active ? { active_null_branch: true } : null)`, treating the `null` branch as a no-op empty object while still compacting static object-literal branches and copying selected members through `sax_json_write_object_members`. Parser coverage preserves the parenthesized ternary spread expression, lowerer coverage checks the generated `OBJ_TERNARY` output plus static `{}` and active object JSON fragments, and composition runtime verification observes the false branch adding no members while the active branch adds `"active_null_branch":true` after parent state updates. The shared top-level ternary scanner now also tracks object and array nesting so literal branch colons are not misparsed as ternary separators. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean-ternary subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999998%: Boolean logical-and object spreads completed for React user component object props. Object spread sources now support the common conditional spread form `...(active && { active_and_branch: true })`, requiring a simple `i1` condition and spreading either a `ptr` JSON object state source or static object literal when true while treating false as a no-op empty object. Parser coverage preserves the logical-and spread expression, lowerer coverage routes it through the same `sax_json_write_object_members` bridge used by ternary spreads and checks static active/no-op JSON fragments, and composition runtime verification observes the false condition adding no members while the active condition adds `"active_and_branch":true` after parent state updates. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999999%: Negated boolean logical-and object spreads completed for React user component object props. Object spread sources now support the common inverse conditional spread form `...(!active && { idle_and_branch: true })`, requiring a simple negated `i1` state condition and spreading either a `ptr` JSON object state source or static object literal only when the condition is false. The lowerer swaps the selected/no-op branches while reusing the existing object-member copy bridge, the plugin `react check` validation now allows this restricted object-spread-only `!state && source` shape without relaxing ordinary interpolation safety, and composition runtime verification observes `"idle_and_branch":true` on inactive renders while active renders omit it. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/negated-logical-and subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999991%: Boolean logical-or fallback object spreads completed for React user component object props. Object spread sources now support the common fallback spread form `...(active || { idle_or_branch: true })`, requiring a simple `i1` state condition and spreading either a `ptr` JSON object state source or static object literal only when the condition is false while treating the true boolean branch as a no-op object spread. Parser coverage preserves the `||` spread expression, the lowerer maps it to the existing selected/no-op object-member copy bridge, plugin `react check` accepts the expression through the normal object-spread validation path, and composition runtime verification observes `"idle_or_branch":true` on inactive renders while active renders omit it. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999992%: Negated boolean logical-or object spreads completed for React user component object props. Object spread sources now support the inverse fallback form `...(!active || { active_or_branch: true })`, requiring a simple negated `i1` state condition and spreading either a `ptr` JSON object state source or static object literal only when the underlying state is true while treating the inactive `!state` branch as a no-op object spread. Parser coverage preserves the negated `||` expression, the lowerer reuses the selected/no-op object-member copy bridge with swapped branch direction, plugin `react check` allows this restricted object-spread-only `!state || source` shape without relaxing ordinary interpolation safety, and composition runtime verification observes `"active_or_branch":true` only after the parent active state flips. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or/negated-logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999993%: Nested dynamic object spreads completed for React user component object props. Object spreads now have parser, lowerer, plugin-check, and composition runtime coverage inside nested object positions, such as `nested: { size: 2, ...nested_extra, ["current"]: count, ...{ enabled: true } }`. The lowerer reuses the same Airlock `sax_json_write_object_members` bridge from nested JSON template buffers, and object normalization preserves JavaScript-style overwrite behavior so later nested members such as `current` and static `enabled` override conflicting spread values while unrelated nested spread members such as `from_nested` remain. The composition demo now drives a nested JSON input and verifies the nested spread before and after the parent active render. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and simple boolean-ternary state subset, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999994%: Array item dynamic object spreads completed for React user component object props. Object spreads now have parser, lowerer, plugin-check, README, and composition runtime coverage inside object literals nested in arrays, such as `items: [{ size: 1, ...nested_extra, ["current"]: count, ...{ enabled: true } }]`. The array-item path reuses the same JSON template buffer and Airlock `sax_json_write_object_members` bridge as top-level and nested-object spreads, and runtime verification confirms JavaScript-style overwrite behavior inside array elements so later `current` and `enabled` members override spread values while unrelated spread members such as `from_nested` remain before and after the parent active render. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999995%: Array item conditional object spreads completed for React user component object props. Object spreads now have parser, lowerer, plugin-check, README, and composition runtime coverage for simple boolean logical spread sources inside object literals nested in arrays, such as `items: [{ size: 1, ...nested_extra, ...(!active && { idle_item: true }), ...(active && { active_item: true }), ["current"]: count }]`. The array-item path reuses the same selected/no-op Airlock `sax_json_write_object_members` branch lowering as top-level spreads, and runtime verification confirms inactive renders include only `idle_item`, active renders include only `active_item`, and existing overwrite behavior still preserves unrelated spread members such as `from_nested`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999996%: Array item ternary null-branch object spreads completed for React user component object props. Object spreads now have parser, lowerer, plugin-check, README, and composition runtime coverage for simple ternary sources with a `null` branch inside object literals nested in arrays, such as `items: [{ size: 1, ...nested_extra, ...(active ? { active_item_branch: true } : null), ["current"]: count }]`. The array-item path reuses the same selected/no-op object-member copy bridge as top-level ternary spreads, and runtime verification confirms inactive renders treat the `null` branch as a no-op while active renders add `active_item_branch` and still preserve unrelated spread members such as `from_nested`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999997%: Array item logical-or object spreads completed for React user component object props. Object spreads now have parser, lowerer, plugin-check, README, and composition runtime coverage for `||` fallback and negated `||` forms inside object literals nested in arrays, such as `items: [{ size: 1, ...nested_extra, ...(active || { idle_item_or: true }), ...(!active || { active_item_or: true }), ["current"]: count }]`. The array-item path reuses the same selected/no-op object-member copy bridge as top-level logical-or spreads, and runtime verification confirms inactive renders include `idle_item_or` but omit `active_item_or`, active renders include `active_item_or` but omit `idle_item_or`, and unrelated spread members such as `from_nested` remain. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999998%: Leading array item dynamic object spreads completed for React user component object props. Object spreads now have parser, lowerer, plugin-check, README, and composition runtime coverage for object literals nested in arrays that begin with a dynamic spread, such as `leading_items: [{ ...nested_extra, ["current"]: count, ...{ enabled: true } }]`. This validates the array-item JSON template path's conditional comma handling when the leading spread source is empty or invalid, and runtime verification confirms later `current` and `enabled` members still overwrite spread values while unrelated spread members such as `from_nested` remain. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999999%: Static string template ternary conditions now support simple truthy scalar state and top-level `&&` composition in `sa_plugin_react`, matching the React-style ownerState class assembly needed by upstream MUI patterns such as `primary && secondary && 'multiline'`. The lowerer now accepts `i1`, `i32`, and `i64` state as truthy conditions for static-string ternary template pieces and supports a restricted top-level `left && right` condition where both operands are simple scalar state names. Coverage includes a dedicated lowerer test for `primary_len && secondary_len ? ' MuiListItemText-multiline' : ''`, and plugin regression verification remains green. Verification: `zig test src/react/lowerer.zig` and `zig build test -j1`.
- 99.9999999999%: Leading array item conditional object spreads completed for React user component object props. Object spreads now have parser, lowerer, plugin-check, README, and composition runtime coverage for object literals nested in arrays that begin with a simple boolean logical spread, such as `leading_conditional_items: [{ ...(!active && { idle_leading_item: true }), current: count }]`. This validates conditional comma emission when the leading spread writes members on inactive renders and becomes a no-op on active renders, with runtime verification confirming active renders produce valid JSON containing only the following `current` member rather than a dangling comma. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999991%: Leading array item ternary null-branch object spreads completed for React user component object props. Object spreads now have parser, lowerer, plugin-check, README, and composition runtime coverage for object literals nested in arrays that begin with a simple ternary spread containing a `null` branch, such as `leading_null_items: [{ ...(active ? { active_leading_branch: true } : null), current: count }]`. This validates conditional comma emission when the leading ternary/null spread writes no members on inactive renders, with runtime verification confirming inactive renders contain only `current` while active renders add `active_leading_branch` before `current` without producing malformed JSON. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999992%: Leading array item positive logical-and object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for object literals nested in arrays that begin with a positive boolean logical spread, such as `leading_active_items: [{ ...(active && { active_leading_item: true }), current: count }]`. This validates conditional comma emission when the leading `&&` spread writes no members on inactive renders and writes `active_leading_item` before `current` on active renders without malformed JSON. The composition demo target object buffer was increased to 2048 bytes after runtime verification exposed that the larger object-prop payload could overflow the prior 1024-byte state buffer and corrupt later DOM handles. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999993%: Leading array item ptr/null ternary object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for object literals nested in arrays that begin with a ternary spread selecting a parent `ptr` JSON object state or `null`, such as `leading_ptr_null_items: [{ ...(active ? nested_extra : null), current: count }]`. This validates conditional comma emission when the leading ternary selects `null` on inactive renders and copies real `nested_extra` object members before the following `current` member on active renders. Runtime verification confirms JavaScript-style overwrite normalization still keeps the later `current` value while preserving copied members such as `enabled` and `from_nested`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999994%: Leading array item ptr/static ternary object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for object literals nested in arrays that begin with a ternary spread selecting either a parent `ptr` JSON object state or a static object literal, such as `leading_ptr_fallback_items: [{ ...(active ? nested_extra : { idle_ptr_fallback: true }), current: count }]`. This validates conditional comma emission when the leading ternary writes static fallback members on inactive renders and copies real `nested_extra` object members on active renders before the following `current` member. Runtime verification confirms object-prop normalization preserves fallback/static members on inactive renders and keeps later `current` overwrite semantics when the active ptr branch contributes its own `current` key. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999995%: Leading array item ptr/ptr ternary object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for object literals nested in arrays that begin with a ternary spread selecting between two parent `ptr` JSON object states, such as `leading_ptr_branch_items: [{ ...(active ? nested_extra : object_branch), current: count }]`. This validates conditional comma emission for both empty and non-empty ptr branches: inactive renders initially fall through to only `current`, then copy real `object_branch` members after the branch input updates, while active renders switch to `nested_extra` and preserve copied `enabled` / `from_nested` members. Runtime verification also confirms later `current` overwrite semantics when either selected ptr branch contributes its own object members. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999996%: Leading array item logical-or ptr fallback object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for object literals nested in arrays that begin with a logical-or spread whose fallback branch is a parent `ptr` JSON object state, such as `leading_ptr_or_items: [{ ...(active || nested_extra), current: count }]`. This validates conditional comma emission when inactive renders select and copy non-empty `nested_extra` members before the following `current` member, while active renders treat the true boolean branch as a no-op and emit only the following member. Runtime verification confirms empty fallback state initially produces valid JSON, populated fallback state copies `enabled` / `from_nested`, and active renders omit the fallback spread without dangling commas. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999997%: Leading array item negated logical-or ptr fallback object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for object literals nested in arrays that begin with a negated logical-or spread whose fallback branch is a parent `ptr` JSON object state, such as `leading_negated_ptr_or_items: [{ ...(!active || nested_extra), current: count }]`. This validates conditional comma emission when inactive renders treat the true negated boolean branch as a no-op and emit only the following `current` member, while active renders select and copy non-empty `nested_extra` members before the following `current` member. Runtime verification confirms populated fallback state stays omitted while inactive, then copies `enabled` / `from_nested` after activation without dangling commas and with later `current` overwrite semantics preserved. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999998%: Array item logical-or ptr fallback object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for non-leading object literals nested in arrays that use logical-or spreads whose fallback branch is a parent `ptr` JSON object state, such as `items: [{ size: 1, ...(active || object_branch), ...(!active || object_branch), current: count }]`. This validates comma-safe object member copying after prior array-item members for both positive and negated `||` forms: inactive renders select and copy the positive fallback object after the branch input is populated, while active renders select and copy the negated fallback object after the click. Runtime verification confirms copied `branch` / `count` / `active` members coexist with existing nested spread members and later static members without malformed JSON. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999999%: Array item logical-and ptr object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for non-leading object literals nested in arrays that use logical-and spreads whose selected branch is a parent `ptr` JSON object state, such as `and_ptr_items: [{ size: 1, ...(active && object_branch), ...(!active && object_branch), current: count }]`. This validates comma-safe no-op handling for false `&&` branches and object-member copying for true `&&` branches after prior array-item members: inactive renders copy the negated branch after the branch input is populated, while active renders copy the positive branch after the click. Runtime verification confirms copied `branch` / `count` / `active` members are omitted before the ptr source is populated, then appear in the expected inactive and active snapshots with later `current` member emission preserved. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999999991%: Leading array item logical-and ptr object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for object literals nested in arrays that begin with logical-and spreads whose selected branch is a parent `ptr` JSON object state, such as `leading_ptr_and_items: [{ ...(active && nested_extra), current: count }]` and `leading_negated_ptr_and_items: [{ ...(!active && nested_extra), current: count }]`. This validates conditional comma emission when leading false `&&` branches write no members and leading true `&&` branches copy non-empty `nested_extra` before the following `current` member. Runtime verification confirms inactive renders copy only the negated branch after nested input is populated, active renders copy only the positive branch after the click, and both no-op branches still produce valid JSON containing only `current`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999999992%: Top-level logical-and ptr object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for top-level logical-and spreads whose selected branch is a parent `ptr` JSON object state, such as `config={{ ...(active && object_branch), ...(!active && object_branch), count: count }}`. This validates no-op handling for false `&&` branches and object-member copying for true `&&` branches in the top-level object-prop template. Runtime verification confirms the inactive negated branch adds `branch` after the branch input is populated while duplicate `count` / `active` keys keep JavaScript-style first-insertion order with later value overwrite semantics, and active renders continue to preserve the existing branch copy path without malformed JSON. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999999993%: Top-level logical-or ptr fallback object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for top-level logical-or spreads whose fallback branch is a parent `ptr` JSON object state, such as `config={{ ...(active || nested_extra), ...(!active || nested_extra), count: count }}`. This validates no-op handling for true positive `||` and false negated `||` branches plus object-member copying for the selected fallback branch in the top-level object-prop template. Runtime verification confirms inactive renders copy `nested_extra` members through the positive `||` spread after the nested input is populated, active renders copy them through the negated `||` spread after the click, and duplicate `enabled` keys preserve JavaScript-style first-insertion order while later spread values overwrite the value. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999999994%: Top-level ptr/null ternary object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for top-level ternary spreads that select a parent `ptr` JSON object state or `null`, such as `config={{ ...(active ? nested_extra : null), count: count }}`. This validates that the inactive `null` branch writes no members in the top-level object-prop template while the active branch copies populated `nested_extra` members without malformed comma emission. Runtime verification confirms active renders copy `current` / `from_nested` immediately after the existing active null-literal branch and overwrite duplicate `enabled` values while preserving first-insertion key order. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999999995%: Top-level ptr/ptr ternary object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for top-level ternary spreads that select between two parent `ptr` JSON object states, such as `config={{ ...(active ? nested_extra : object_branch), count: count }}`. This validates both selected branches in the top-level object-prop template: inactive renders copy populated `object_branch` members before later conditional spreads, while active renders switch to populated `nested_extra` members without malformed comma emission. Runtime verification confirms the inactive branch first introduces `branch` / `active` before `idle_and_branch`, and active renders preserve the selected `nested_extra` key order and duplicate-overwrite behavior. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999999996%: Nested logical-and ptr object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for nested object literals that use positive and negated logical-and spreads whose selected branch is a parent `ptr` JSON object state, such as `nested: { ...nested_extra, ...(active && object_branch), ...(!active && object_branch), current: count }`. This validates comma-safe no-op handling and object-member copying inside nested object-prop templates rather than only at top-level or array-item positions. Runtime verification confirms inactive renders copy `object_branch` members into `nested` through the negated branch, active renders copy the same populated source through the positive branch, and later nested members such as `current`, `enabled`, and `spread_active` preserve overwrite behavior and stable first-insertion order. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999999997%: Nested logical-or ptr fallback object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for nested object literals that use positive and negated logical-or spreads whose fallback branch is a parent `ptr` JSON object state, such as `nested: { ...nested_extra, ...(active || object_branch), ...(!active || object_branch), current: count }`. This validates no-op handling for true positive `||` and false negated `||` branches plus object-member copying inside nested object-prop templates rather than only at top-level or array-item positions. Runtime verification continues to cover inactive and active nested branch-member copies through the shared normalized JSON path, with the added nested `||` branches exercising the same parent-ptr fallback code path without changing final duplicate-key order. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999999998%: Nested ptr/null and ptr/ptr ternary object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for nested object literals that use ternary spreads selecting a parent `ptr` JSON object state, another parent `ptr` JSON object state, or `null`, such as `nested: { ...(active ? nested_extra : object_branch), ...(active ? nested_extra : null), ...nested_extra, current: count }`. This validates nested-template comma emission for null no-op branches and both selected ptr branches, rather than only top-level, array-item, or leading-array positions. Runtime verification confirms inactive renders copy `object_branch` members through the nested ptr/ptr false branch before later `nested_extra` members, while active renders select `nested_extra` through both ternary spreads and preserve existing duplicate-key overwrite behavior. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.999999999999%: Nested ptr/static ternary object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for nested object literals that use ternary spreads selecting a parent `ptr` JSON object state or a static object-literal fallback, such as `nested: { ...(active ? nested_extra : { idle_nested_branch: true }), current: count }`. This validates nested-template static fallback compaction and comma-safe member emission for inactive renders, while active renders copy the ptr branch without leaving the fallback key behind. Runtime verification confirms inactive snapshots include `idle_nested_branch` inside `nested` before later dynamic/static members, while active snapshots omit it and continue to preserve duplicate-key overwrite behavior. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999999991%: Nested static conditional object spreads completed for React user component object props. Object spreads now have parser, lowerer, README, plugin-check, and composition runtime coverage for nested object literals that use static object-literal branches behind ternary-null, logical-and, and logical-or conditions, such as `nested: { ...(active ? { active_nested_branch: true } : null), ...(active && { active_nested_and: true }), ...(!active && { idle_nested_and: true }), ...(active || { idle_nested_or: true }), ...(!active || { active_nested_or: true }), current: count }`. This validates adjacent nested spread parsing without phantom empty text pieces, static branch compaction in nested JSON templates, and comma-safe no-op handling for inactive/active logical branches. Runtime verification confirms inactive snapshots include `idle_nested_and` and `idle_nested_or`, active snapshots include `active_nested_branch`, `active_nested_and`, and `active_nested_or`, and later nested ptr/static members preserve overwrite behavior and stable first-insertion order. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999999992%: Nested dynamic computed object keys completed for React user component object props. Computed keys now have parser, lowerer, README, and composition runtime coverage inside nested object literals, including string-literal ternary keys, numeric ternary keys, and ptr-string/null-style ternary keys such as `nested: { [active ? "nested_ready" : "nested_idle"]: count, [active ? count : idle_score]: active, [active ? title : fallback]: count, current: count }`. This validates that nested object-prop templates use the same JSON key emitters and normalization path as top-level object props, including quoted string escaping, numeric key formatting, and JavaScript-style duplicate overwrite with stable first-insertion order. Runtime verification confirms inactive nested snapshots include `"9":false`, `"nested_idle":0`, and either the empty or fallback string key as inputs change, while active snapshots include `"1":true`, `"nested_ready":1`, and `"click":1`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999999993%: Array item dynamic computed object keys completed for React user component object props. Computed keys now have parser, lowerer, README, and composition runtime coverage inside object literals nested in arrays, including string-literal ternary keys, numeric ternary keys, and ptr-string ternary keys such as `computed_items: [{ size: 1, [active ? "item_ready" : "item_idle"]: count, [active ? count : idle_score]: active, [active ? title : fallback]: count, current: count }]`. This validates that array-item object templates use the same JSON key emitters and normalization path as top-level and nested object props, including quoted string escaping, numeric key formatting, and JavaScript-style duplicate overwrite with stable first-insertion order. Runtime verification confirms inactive array-item snapshots include `"9":false`, `"item_idle":0`, and either the empty or fallback string key as inputs change, while active snapshots include `"1":true`, `"item_ready":1`, and `"click":1`; the composition demo's object config buffer was increased to keep the larger JSON payload within its allocated child prop state. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999999994%: Leading array item dynamic computed object keys completed for React user component object props. Computed keys now have parser, lowerer, README, and composition runtime coverage when the first member of an object literal nested in an array is a computed key, such as `leading_computed_items: [{ [active ? "leading_item_ready" : "leading_item_idle"]: count, [active ? count : idle_score]: active, [active ? title : fallback]: count, current: count }]`. This validates that array-item object templates emit valid JSON without a leading comma or missing separator when a computed key starts the object, while still using the shared JSON key emitters, string escaping, numeric key formatting, and normalization path. Runtime verification confirms inactive leading array-item snapshots include `"9":false`, `"leading_item_idle":0`, and either the empty or fallback string key as inputs change, while active snapshots include `"1":true`, `"leading_item_ready":1`, and `"click":1`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999999995%: Leading nested dynamic computed object keys completed for React user component object props. Computed keys now have parser, lowerer, README, and composition runtime coverage when the first member of a nested object literal is a computed key, such as `leading_nested: { [active ? "leading_nested_ready" : "leading_nested_idle"]: count, [active ? count : idle_score]: active, [active ? title : fallback]: count, current: count }`. This validates that nested object-prop templates emit valid JSON without a leading comma or missing separator when a computed key starts the nested object, while still using the shared JSON key emitters, string escaping, numeric key formatting, and normalization path. Runtime verification confirms inactive leading nested snapshots include `"9":false`, `"leading_nested_idle":0`, and either the empty or fallback string key as inputs change, while active snapshots include `"1":true`, `"leading_nested_ready":1`, and `"click":1`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999999996%: Dynamic computed keys inside static object spread literals completed for React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage for computed keys inside the spread body, such as `config={{ ...{ [active ? "spread_ready" : "spread_idle"]: count, spread_count: count }, count: count }}`. This validates that flattened static object spreads reuse the same computed-key JSON template path as ordinary object members, including ternary string key selection, scalar value formatting, and normalized duplicate-key behavior. Runtime verification confirms inactive snapshots include `"spread_idle":0` before the static spread members and active snapshots include `"spread_ready":1` before `"spread":"static"` / `"spread_count":1`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999999997%: Nested and array-item static spread literal computed keys completed for React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage for computed keys inside spread bodies that are flattened into nested object and array item object positions, such as `nested: { ...{ [active ? "nested_spread_ready" : "nested_spread_idle"]: count, spread_active: active } }` and `items: [{ ...{ [active ? "nested_spread_ready" : "nested_spread_idle"]: count, spread_active: active } }]`. This validates that nested and array-item spread-literal flattening reuses the same computed-key JSON template path as top-level static spreads, including ternary string key selection, scalar value formatting, comma-safe member emission, and normalized duplicate-key behavior. Runtime verification confirms inactive snapshots include `"nested_spread_idle":0` inside both `items[0]` and `nested`, while active snapshots include `"nested_spread_ready":1` in both positions before their final `spread_active` members. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999999998%: Leading array-item static spread literal computed keys completed for React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage when an object literal nested in an array begins with a spread body containing a computed key, such as `leading_static_spread_items: [{ ...{ [active ? "leading_spread_ready" : "leading_spread_idle"]: count, enabled: true, spread_active: active }, current: count }]`. This validates that leading static spread-literal flattening emits the computed key and following ordinary members without a leading comma, while preserving the shared JSON template interpolation path, ternary string key selection, scalar value formatting, and normalized duplicate-key behavior. Runtime verification confirms inactive snapshots include `"leading_spread_idle":0` before `enabled`, `spread_active`, and `current`, while active snapshots include `"leading_spread_ready":1` with the same member order. The Airlock generator also now supplies the compiler runtime `write` and `exit` imports required by current wasm output. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.9999999999999%: Scalar computed keys inside static spread literals completed for React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage for arithmetic computed keys inside the spread body, such as `config={{ ...{ [active ? "spread_ready" : "spread_idle"]: count, [count + 100]: active, spread_count: count }, count: count }}`. This validates that flattened static object spreads reuse the same computed-key JSON formatting path for scalar arithmetic keys as ordinary object members, not only string ternary keys. Runtime verification confirms inactive snapshots include the normalized integer-style top-level key `"100":false`, while active snapshots include `"101":true`; both appear in JavaScript object integer-key order near the front of the normalized JSON object. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999999991%: Nested and array-item scalar computed keys inside static spread literals completed for React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage for arithmetic computed keys inside spread bodies that are flattened into nested object and array item positions, such as `nested: { ...{ [active ? "nested_spread_ready" : "nested_spread_idle"]: count, [count + 200]: active, spread_active: active } }` and `items: [{ ...{ [active ? "nested_spread_ready" : "nested_spread_idle"]: count, [count + 200]: active, spread_active: active } }]`. This validates that nested and array-item static spread flattening use the same scalar computed-key JSON formatting path as top-level spreads and ordinary object members. Runtime verification confirms inactive snapshots include `"200":false` inside both `items[0]` and `nested`, while active snapshots include `"201":true` in both positions, with integer-style keys normalized near the front of each object. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999999992%: Leading array-item scalar computed keys inside static spread literals completed for React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage when an array item object begins with a flattened static spread body containing both string and arithmetic computed keys, such as `leading_static_spread_items: [{ ...{ [active ? "leading_spread_ready" : "leading_spread_idle"]: count, [count + 300]: active, enabled: true, spread_active: active }, current: count }]`. This validates that leading static spread-literal flattening emits scalar computed keys without a leading comma and still separates later ordinary members correctly. Runtime verification confirms inactive snapshots include `"300":false` before `"leading_spread_idle":0` inside `leading_static_spread_items[0]`, while active snapshots include `"301":true` before `"leading_spread_ready":1`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999999993%: Leading nested-object scalar computed keys inside static spread literals completed for React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage when a nested object begins with a flattened static spread body containing both string and arithmetic computed keys, such as `leading_static_spread_nested: { ...{ [active ? "leading_nested_spread_ready" : "leading_nested_spread_idle"]: count, [count + 400]: active, enabled: true, spread_active: active }, current: count }`. This validates that leading nested static spread-literal flattening emits scalar computed keys without a leading comma and still separates later ordinary members correctly. Runtime verification confirms inactive snapshots include `"400":false` before `"leading_nested_spread_idle":0` inside `leading_static_spread_nested`, while active snapshots include `"401":true` before `"leading_nested_spread_ready":1`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999999994%: Boolean computed keys inside static spread literals completed for nested and array-item React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage for spread bodies that include boolean state keys such as `items: [{ ...{ [active ? "nested_spread_ready" : "nested_spread_idle"]: count, [count + 200]: active, [active]: count, enabled: true, spread_active: active } }]`, `nested: { ...{ [active ? "nested_spread_ready" : "nested_spread_idle"]: count, [count + 200]: active, [active]: count, enabled: true, spread_active: active } }`, and leading array/nested static-spread positions. This validates that flattened static spreads reuse the same quoted boolean key emitter as ordinary object members while preserving comma-safe member emission and duplicate-key insertion order when earlier spreads already introduced keys such as `enabled`. Runtime verification confirms inactive snapshots include `"false":0` inside `items[0]`, `nested`, `leading_static_spread_items[0]`, and `leading_static_spread_nested`, while active snapshots include `"true":1` in the same positions. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999999995%: Top-level boolean computed keys inside static spread literals completed for React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage for top-level spread bodies that include a boolean state key alongside string and arithmetic computed keys, such as `config={{ ...{ [active ? "spread_ready" : "spread_idle"]: count, [count + 100]: active, [active]: count, spread_count: count }, count: count }}`. This validates that top-level flattened static spreads use the same quoted boolean key emitter as ordinary object members and preserve JavaScript-style duplicate-key semantics when a later static-spread boolean key overwrites an earlier top-level computed boolean key without changing the original insertion order. Runtime verification confirms inactive snapshots now keep the existing top-level `"false"` slot but update its value from the earlier `idle_score` write to `0`, while active snapshots keep the top-level `"true"` slot and update its value to `1`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999999996%: Boolean ternary computed keys inside static spread literals completed for React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage for spread bodies that use simple `i1_state ? i1_state : i1_state` and `i1_state ? i1_state : i1_literal` computed keys, such as `ternary_static_spread_items: [{ ...{ [active ? active : fallback_active]: idle_score, [active ? fallback_active : true]: count, current: count } }]`. This validates that flattened static spread bodies reuse the `KEY_I1_TERNARY` branch path for quoted boolean object keys rather than only direct boolean state keys, while preserving leading spread comma handling, duplicate-key overwrite semantics, and following member emission in array-item object templates. Runtime verification confirms inactive snapshots include `ternary_static_spread_items:[{"false":9,"true":0,"current":0}]`, while active snapshots include `ternary_static_spread_items:[{"true":9,"false":1,"current":1}]`. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.
- 99.99999999999997%: Boolean literal-only ternary computed keys inside static spread literals completed for React user component object props. Static spread literals now have parser, lowerer, README, and composition runtime coverage for spread bodies that include `i1_state ? i1_literal : i1_literal` computed keys, such as `ternary_static_spread_items: [{ ...{ [active ? active : fallback_active]: idle_score, [active ? fallback_active : true]: count, [active ? false : true]: count, current: count } }]`. This validates that flattened static spread bodies can branch between quoted boolean literal keys without a state-backed value branch, and that duplicate-key insertion order remains stable when the literal-only branch rewrites the same `"true"` or `"false"` slot introduced by an earlier computed key. Runtime verification keeps the normalized inactive snapshot at `ternary_static_spread_items:[{"false":9,"true":0,"current":0}]` and active snapshot at `ternary_static_spread_items:[{"true":9,"false":1,"current":1}]`, with the later duplicate literal branch preserving key order while refreshing the value. General JavaScript computed object key expressions beyond the current scalar arithmetic/state/string-literal/null-literal-ternary/ptr-string-ternary/i64-ternary/i64-literal-branch-ternary/i32-ternary/i32-literal-branch-ternary/i1-ternary/i1-literal-branch-ternary/i1-literal-only-ternary/f64-ternary/f64-literal-branch-ternary subset, arbitrary JavaScript object spread expressions beyond the current ptr-state/static-literal/null-literal and simple boolean ternary/logical-and/logical-or subset, complex non-scalar object/string expressions beyond the current ptr-state, string-literal, null-literal, numeric/boolean/float/i32-state, simple numeric/i32/float/boolean literal branch, and general JavaScript object semantics remain follow-up work. Verification: `sa version`, `zig test src/react/parser.zig`, `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, `zig test tools/verify_react_demo_wasm.zig`, and `zig build test -j1`.

## Latest Verification

- Added generic ptr-state static string initialization in the React lowerer, so SA/SAX components can declare defaults like `'bottom' as ptr` and have that ownerState string materialized into heap-backed state during init. This keeps default string ownerState in `sa_plugin_mui` on the React/SAX path instead of forcing hardcoded default classes in templates. Verification: `zig test src/react/lowerer.zig`, `zig build test -j1`, plus downstream MUI `sa react check/build` and Chromium browser verification.
- Added generic static string template condition support for `ptr_state == 'literal'`, lowered through the shared `sax_mem_eq` airlock bridge instead of any MUI-specific path. This keeps string ownerState class branches such as `position == 'top'` in `sa_plugin_mui` expressible through the same React/SAX lowering surface as upstream React code. Verification: `zig test src/react/lowerer.zig`, `zig test src/react/airlock_gen.zig`, and `zig build test -j1`.

- Added generic mixed text-and-element DOM child lowering for React/SAX nodes. When a DOM element contains both text children and element children, such as `{title}<span />`, the lowerer now allocates stable text-node DOM slots, appends them in child order, and updates those text nodes during render instead of setting parent `textContent` and deleting sibling elements. The Node runtime verifier now supports `document.createTextNode`, and lowerer coverage locks the mixed child case. This keeps MUI `Tooltip` title/arrow behavior in the shared React/SAX layer rather than adding MUI-specific logic. Verification: `zig test src/react/lowerer.zig`, `zig build test -j1`, `zig build`, plus downstream MUI `sa react check/build` and Chromium browser verification.

- `sa version`
- `zig test src/react/parser.zig`
- `zig test src/react/lowerer.zig`
- `zig test src/react/airlock_gen.zig`
- `zig test tools/verify_react_demo_wasm.zig`
- `zig build test -j1`

## Remaining Scope Notes

- Not complete React 1:1 yet: Fiber/hooks/class lifecycle parity, full keyed reconciliation, full React component instance ref semantics, general JavaScript computed object key expressions, arbitrary JavaScript object spread expressions, general JS expressions, and broader runtime scheduling remain open.
